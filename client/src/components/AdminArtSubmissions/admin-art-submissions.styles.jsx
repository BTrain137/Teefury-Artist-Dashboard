import styled, { css } from "styled-components";

export const EmailIconLegend = styled.div`
  background-color: rgba(0, 0, 0, 0.5);
  font-size: 12px;
  color: #ffffff;
`;

export const EmailIconLegendSpan = styled.span`
  display: flex;
  align-items: center;
`;
